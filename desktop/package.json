{
  "name": "desktop",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron": "electron .",
    "pack": "electron-packager . --platform=darwin --arch=arm64 --out=dist"
  },
  "build": {
    "appId": "com.zero.0",
    "mac": {
      "category": "public.app-category.utilities"
    }
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "typescript": "^5.3.3"
  }
}
